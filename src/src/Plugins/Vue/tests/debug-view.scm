;; the Clay debug view (F1) over a window with a tool: it reads the id strings of the elements
(use-modules (texmacs menus preferences-widgets))
(delayed (:pause 3000) (tool-select :right 'preferences-tool))
